---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import behandlungData from '../data/behandlung.json';
---

<Layout 
  title={`${behandlungData.page_title} | Klangraum in Balance`}
  description={behandlungData.how_it_works_text.slice(0, 160) + "..."}
>
  <Header />
  
  <main id="main-content">
    <!-- Page Header -->
    <section class="pt-16 md:pt-24 pb-8">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-xl md:text-2xl text-klang-green">
          {behandlungData.page_title}
        </h1>
      </div>
    </section>
    
    <!-- Section 1: Wie die Klangmassage wirkt -->
    <section 
      class="py-12 md:py-20"
      aria-labelledby="how-it-works-heading"
    >
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start">
          <!-- Text -->
          <div class="order-2 lg:order-1">
            <h2 
              id="how-it-works-heading"
              class="section-heading text-lg md:text-xl text-klang-green"
            >
              {behandlungData.how_it_works_heading}
            </h2>
            <div class="prose prose-lg max-w-none">
              <p class="text-klang-green/90 text-lg md:text-xl leading-relaxed">
                {behandlungData.how_it_works_text}
              </p>
            </div>
          </div>
          <!-- Image -->
          <div class="order-1 lg:order-2">
            <div class="relative rounded-2xl overflow-hidden shadow-lg">
              <img 
                src={behandlungData.how_it_works_image} 
                alt={behandlungData.how_it_works_image_alt} 
                class="w-full h-auto object-cover aspect-video"
                width="917"
                height="620"
                loading="lazy"
              />
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Section 2: So läuft die Klangmassage ab -->
    <section 
      class="py-12 md:py-20 bg-white/50"
      aria-labelledby="procedure-heading"
    >
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
          <!-- Image -->
          <div class="order-1 lg:order-1">
            <div class="relative rounded-2xl overflow-hidden shadow-xl">
              <img 
                src={behandlungData.procedure_image} 
                alt={behandlungData.procedure_image_alt} 
                class="w-full h-auto object-cover"
                width="1264"
                height="848"
                loading="lazy"
              />
            </div>
          </div>
          
          <!-- Text -->
          <div class="order-2 lg:order-2">
            <h2 
              id="procedure-heading"
              class="section-heading text-lg md:text-xl text-klang-green"
            >
              {behandlungData.procedure_heading}
            </h2>
            <div class="prose prose-lg max-w-none">
              <p class="text-klang-green/90 text-lg md:text-xl leading-relaxed">
                {behandlungData.procedure_text}
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Section 3: Behandlungsdauer & Kosten -->
    <section 
      class="py-12 md:py-20"
      aria-labelledby="pricing-heading"
    >
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 
          id="pricing-heading"
          class="section-heading text-lg md:text-xl text-klang-green text-center"
        >
          {behandlungData.pricing_heading}
        </h2>
        <div class="prose prose-lg max-w-none">
          <p class="text-klang-green/90 text-lg md:text-xl leading-relaxed">
            {behandlungData.pricing_text}
          </p>
        </div>
      </div>
    </section>
    
    <!-- CTA Section -->
    <section 
      class="py-16 md:py-24 bg-[#5a6b5c] text-klang-cream"
      aria-labelledby="cta-heading"
    >
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 
          id="cta-heading"
          class="text-lg md:text-xl mb-6 text-klang-cream"
        >
          {behandlungData.cta_heading}
        </h2>
        <p class="text-xl md:text-2xl text-klang-cream/90 mb-8 max-w-2xl mx-auto">
          {behandlungData.cta_text}
        </p>
        <a 
          href="/kontakt/"
          class="inline-flex items-center justify-center px-8 py-4 bg-klang-cream text-[#5a6b5c] font-semibold rounded-lg hover:bg-klang-cream/90 transition-colors focus:outline-none focus:ring-2 focus:ring-klang-cream/50"
          aria-label={behandlungData.cta_button}
        >
          {behandlungData.cta_button}
        </a>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>
