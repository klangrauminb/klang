---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const legalPages = await Astro.glob('../content/*.md');
const impressum = legalPages.find(p => p.file.includes('impressum'));
---

<Layout 
  title={impressum?.frontmatter.title ? `${impressum.frontmatter.title} | Klangraum in Balance` : "Impressum | Klangraum in Balance"}
  description={impressum?.frontmatter.description || "Impressum von Klangraum in Balance."}
>
  <Header />
  
  <main id="main-content">
    <!-- Page Header -->
    <section class="pt-16 md:pt-24 pb-8">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-xl md:text-2xl text-klang-green">
          {impressum?.frontmatter.title || "Impressum"}
        </h1>
      </div>
    </section>
    
    <!-- Content -->
    <section class="py-12 md:py-20">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="prose prose-lg max-w-none text-klang-green" set:html={impressum?.compiledContent()} />
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>
